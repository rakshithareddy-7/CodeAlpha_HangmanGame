# CodeAlpha_HangmanGame
A simple text-based Hangman Game built using Python. The project uses loops, conditions, lists, strings, and the random module to allow users to guess hidden words with limited attempts.
